{
  "name": "sumarket-express-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env --watch src/server.js",
    "start": "node --env-file=.env src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@prisma/client": "^7.9.1",
    "cors": "^2.8.6",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "prisma": "^7.9.1",
    "supertest": "^7.1.1"
  }
}
