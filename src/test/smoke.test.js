import { describe, it, expect } from "vitest";

// Test "de humo": solo confirma que Vitest está bien configurado y corriendo.
// Se reemplaza por tests reales del carrito en el siguiente chunk.
describe("configuración de Vitest", () => {
  it("corre y evalúa una aserción básica", () => {
    expect(1 + 1).toBe(2);
  });
});
