# Cloudflare Pages — configuración del proyecto.
# `pages_build_output_dir` indica la carpeta que Vite genera tras `npm run build`.
name = "sumarketexpress"
pages_build_output_dir = "./dist"
compatibility_date = "2025-04-01"
